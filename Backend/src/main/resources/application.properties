spring.application.name=Backend
server.port=9090

# Database — H2 in-memory by default for local development.
# Switch to PostgreSQL by setting SPRING_PROFILES_ACTIVE=postgres.
spring.datasource.url=jdbc:h2:mem:backenddb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.open-in-view=false
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Flyway (disabled for H2 in-memory — schema is generated via ddl-auto=update)
spring.flyway.enabled=false

# OpenAPI / Swagger UI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.try-it-out-enabled=true

# Mappls API
# Base URL stays here; the access token lives in mappls-credentials.properties
# (loaded via @PropertySource on BackendApplication) so updates only require
# editing that file and restarting the backend.
mappls.api.base-url=https://search.mappls.com

# Jackson — Spring Boot 4.1 ships Jackson 3.x. WRITE_DATES_AS_TIMESTAMPS
# was removed in 3.x (the default is now false), so we don't set it.
spring.jackson.default-property-inclusion=NON_NULL

# Redis — disabled for local dev. PropertyService uses @Cacheable which
# falls back to a no-op cache manager when redis is not configured.
spring.cache.type=none
spring.data.redis.repositories.enabled=false

# JWT Security
app.security.jwt-secret=${JWT_SECRET:change-this-development-secret-key-at-least-32-characters-long}
app.security.jwt-expiration-ms=3600000

# Logging
logging.level.org.springframework.security=INFO
logging.level.com.duedilligenceagent=DEBUG
